{
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd wallet-watch-frontend && npm start",
    "backend": "cd wallet-watch-backend && npm start",
    "build": "tsc --sourcemap ./wallet-watch-backend/AppServer.ts"
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
